import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";
test("optional inventory and costs are tenant isolated immutable projections of one operation",async()=>{const sql=await readFile(new URL("../migrations/0017_optional_inventory_costs.sql",import.meta.url),"utf8");for(const table of["inventory_products","inventory_lots","operation_consumptions","inventory_movements","farm_costs"])assert.match(sql,new RegExp(`${table}_tenant_isolation`));assert.match(sql,/operation_id[^\n]+REFERENCES "farm"\."operations"/);assert.match(sql,/inventory_movements_immutable/);assert.match(sql,/operation_consumptions_org_operation_product_unique/);assert.match(sql,/REVOKE DELETE/)});
