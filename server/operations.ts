import{randomUUID}from"node:crypto";import{asc,eq,sql}from"drizzle-orm";import{farmAuditEvents,farmContractors,farmEquipment,farmFields,farmOperations,farmWorkers,operationContractors,operationEquipment,operationSequences,operationWorkers,plantations,type FarmOperation}from"@shared/schema";import type{CreateOperationInput,OperationDto}from"@shared/operations";import type{FarmDatabase}from"./database";import{withOrganizationTransaction}from"./database";import type{FarmRequestContext}from"./farm-context";
export interface OperationRepository{list(context:FarmRequestContext):Promise<OperationDto[]>;create(context:FarmRequestContext,input:CreateOperationInput):Promise<OperationDto>};const base=(row:FarmOperation)=>({id:row.id,code:row.code,fieldId:row.fieldId,plantationId:row.plantationId||undefined,type:row.type as OperationDto["type"],performedAt:row.performedAt.toISOString(),areaHa:row.areaHa?Number(row.areaHa):undefined,durationMinutes:row.durationMinutes?Number(row.durationMinutes):undefined,notes:row.notes||undefined,status:row.status as OperationDto["status"],createdAt:row.createdAt.toISOString()});
export function createPostgresOperationRepository(db:FarmDatabase):OperationRepository{return{async list(context){return withOrganizationTransaction(db,context.organization.id,async(tx)=>{const[rows,w,e,c]=await Promise.all([tx.select().from(farmOperations).orderBy(asc(farmOperations.performedAt)),tx.select().from(operationWorkers),tx.select().from(operationEquipment),tx.select().from(operationContractors)]);return rows.map((row)=>({...base(row),workerIds:w.filter((item)=>item.operationId===row.id).map((item)=>item.workerId),equipmentIds:e.filter((item)=>item.operationId===row.id).map((item)=>item.equipmentId),contractorIds:c.filter((item)=>item.operationId===row.id).map((item)=>item.contractorId)}))})},async create(context,input){return withOrganizationTransaction(db,context.organization.id,async(tx)=>{const[field]=await tx.select({id:farmFields.id,usableAreaHa:farmFields.usableAreaHa}).from(farmFields).where(eq(farmFields.id,input.fieldId));if(!field)throw Object.assign(new Error("Field not found"),{status:404,code:"FIELD_NOT_FOUND"});if(input.areaHa&&input.areaHa>Number(field.usableAreaHa))throw Object.assign(new Error("Operation area exceeds usable field area"),{status:400,code:"OPERATION_AREA_INVALID"});if(input.plantationId){const[p]=await tx.select({fieldId:plantations.fieldId}).from(plantations).where(eq(plantations.id,input.plantationId));if(!p||p.fieldId!==input.fieldId)throw Object.assign(new Error("Plantation does not belong to field"),{status:400,code:"OPERATION_PLANTATION_INVALID"})}for(const[ids,table,label]of[[input.workerIds,farmWorkers,"WORKER"],[input.equipmentIds,farmEquipment,"EQUIPMENT"],[input.contractorIds,farmContractors,"CONTRACTOR"]]as const)for(const resourceId of ids){const[row]=await tx.select({id:table.id}).from(table).where(eq(table.id,resourceId));if(!row)throw Object.assign(new Error(`${label} not found`),{status:400,code:`OPERATION_${label}_INVALID`})}const year=new Date(input.performedAt).getUTCFullYear();const result=await tx.execute(sql`insert into ${operationSequences} (organization_id,year,next_value) values (${context.organization.id},${year},2) on conflict (organization_id,year) do update set next_value=${operationSequences.nextValue}+1 returning next_value-1 as value`);const value=Number((result.rows[0]as{value:string}).value);const code=`OP-${year}-${String(value).padStart(6,"0")}`;const id=randomUUID();const[created]=await tx.insert(farmOperations).values({id,organizationId:context.organization.id,code,fieldId:input.fieldId,plantationId:input.plantationId,type:input.type,performedAt:new Date(input.performedAt),areaHa:input.areaHa?String(input.areaHa):null,durationMinutes:input.durationMinutes?String(input.durationMinutes):null,notes:input.notes}).returning();if(input.workerIds.length)await tx.insert(operationWorkers).values(input.workerIds.map((workerId)=>({operationId:id,workerId,organizationId:context.organization.id})));if(input.equipmentIds.length)await tx.insert(operationEquipment).values(input.equipmentIds.map((equipmentId)=>({operationId:id,equipmentId,organizationId:context.organization.id})));if(input.contractorIds.length)await tx.insert(operationContractors).values(input.contractorIds.map((contractorId)=>({operationId:id,contractorId,organizationId:context.organization.id})));await tx.insert(farmAuditEvents).values({id:randomUUID(),organizationId:context.organization.id,actorUserId:context.user.id,action:"operation.performed",entityType:"operation",entityId:id,metadata:{code,type:input.type}});return{...base(created),workerIds:input.workerIds,equipmentIds:input.equipmentIds,contractorIds:input.contractorIds}})}}}
