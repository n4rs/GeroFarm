import{z}from"zod";export const operationTypes=["soil_preparation","crop_installation","cultural_work","fertilization","spraying","irrigation","fertigation","monitoring","harvest","other"]as const;export const soilPreparationActionIds=["subsoiling","ploughing","scarifying","harrowing","rotary_tilling","levelling","bed_forming","furrow_opening","stone_removal","residue_shredding","residue_incorporation","solarisation"]as const;export const soilConditions=["dry","moist","wet"]as const;export const residueDestinations=["left","shredded","incorporated","removed","burned","other"]as const;export const operationDestinationSchema=z.object({fieldId:z.string().uuid(),plantationId:z.string().uuid().optional(),areaHa:z.number().positive().max(1_000_000),percentage:z.number().positive().max(100)});export const soilPreparationSchema=z.object({actions:z.array(z.string().trim().min(2).max(120)).min(1).max(20),depthCm:z.number().positive().max(300).optional(),passes:z.number().int().positive().max(100).optional(),soilCondition:z.enum(soilConditions).optional(),residueDestination:z.enum(residueDestinations).optional()});export const createOperationSchema=z.object({destinations:z.array(operationDestinationSchema).min(1).max(100).superRefine((rows,context)=>{const total=rows.reduce((sum,row)=>sum+row.percentage,0);if(Math.abs(total-100)>0.01)context.addIssue({code:"custom",message:"Destination percentages must total 100"})}),type:z.enum(operationTypes),performedAt:z.string().datetime({offset:true}),durationMinutes:z.number().int().positive().max(10080).optional(),notes:z.string().trim().max(2000).optional(),workerIds:z.array(z.string().uuid()).max(50).default([]),equipmentIds:z.array(z.string().uuid()).max(50).default([]),contractorIds:z.array(z.string().uuid()).max(20).default([]),soilPreparation:soilPreparationSchema.optional()}).superRefine((value,context)=>{if(value.type==="soil_preparation"&&!value.soilPreparation)context.addIssue({code:"custom",path:["soilPreparation"],message:"Soil preparation details are required"});if(value.type!=="soil_preparation"&&value.soilPreparation)context.addIssue({code:"custom",path:["soilPreparation"],message:"Soil preparation details require the matching operation type"})});export type CreateOperationInput=z.infer<typeof createOperationSchema>;export type OperationDto=Omit<CreateOperationInput,"workerIds"|"equipmentIds"|"contractorIds">&{id:string;code:string;status:"performed"|"voided";workerIds:string[];equipmentIds:string[];contractorIds:string[];createdAt:string};
