import test from"node:test";import assert from"node:assert/strict";import{supportedLocales}from"../../home-copy";import{notebookExportLabels}from"./notebook-export-locales";test("XLSX notebook action is explicit in all 28 locales",()=>{assert.deepEqual(Object.keys(notebookExportLabels),[...supportedLocales]);for(const locale of supportedLocales)assert.ok(notebookExportLabels[locale].trim())});
