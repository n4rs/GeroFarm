import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import test from"node:test";test("product applications remain one immutable tenant-scoped physical operation",async()=>{const sql=await readFile(new URL("../migrations/0014_product_applications.sql",import.meta.url),"utf8");assert.match(sql,/operation_id.*PRIMARY KEY/s);assert.match(sql,/REFERENCES "farm"\."operations"/);assert.match(sql,/ENABLE ROW LEVEL SECURITY/);assert.match(sql,/FORCE ROW LEVEL SECURITY/);assert.match(sql,/REVOKE DELETE/);assert.match(sql,/warnings_accepted/);assert.match(sql,/legal_applicator_worker_id/);assert.match(sql,/spray_volume_l_ha/);});
