import assert from"node:assert/strict";import test from"node:test";import{createOperationSchema}from"./operations";test("one physical operation accepts shared resource projections",()=>{const result=createOperationSchema.parse({fieldId:crypto.randomUUID(),type:"cultural_work",performedAt:"2026-08-24T09:00:00+01:00",workerIds:[crypto.randomUUID()],equipmentIds:[crypto.randomUUID()]});assert.equal(result.workerIds.length,1);assert.equal(result.contractorIds.length,0)});
