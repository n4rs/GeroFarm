import assert from "node:assert/strict";import test from "node:test";import { createNotebookPdf,createNotebookXlsx,notebookProjection } from "./agronomy";
test("closed notebook generator returns a structurally valid PDF",()=>{const pdf=createNotebookPdf(["GeroFarm","EXP-2026-0001-V1","Integrity: abc"]);assert.equal(pdf.subarray(0,8).toString(),"%PDF-1.4");assert.match(pdf.toString(),/startxref[\s\S]+%%EOF$/);});
test("field notebook projects each harvest once and excludes economic data",()=>{const projected=notebookProjection({sections:{operations:[{id:"op-1",type:"harvest"},{id:"op-2",type:"irrigation",resourceSnapshot:{water:10}}],harvests:[{id:"h-1",operationId:"op-1",totalNetKg:20,commercialSnapshot:{revenue:30}}]}});const sections=projected.sections as {operations:unknown[];harvests:Array<Record<string,unknown>>};assert.equal(sections.operations.length,1);assert.equal(sections.harvests.length,1);assert.equal("commercialSnapshot" in sections.harvests[0],false)});
test("XLSX analysis export is a valid workbook without economic harvest data",()=>{const file=createNotebookXlsx({sections:{operations:[{id:"op-1",type:"fertilization"}],monitorings:[],laboratorySamples:[],harvests:[{id:"h-1",lotCode:"LOT1",revenue:500,cost:100,margin:400,commercialSnapshot:{price:2}}]}});assert.equal(file[0],0x50);assert.equal(file[1],0x4b);assert.ok(file.length>1000)});
